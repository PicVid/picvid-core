sudo: false

env:
  CODECLIMATE_REPO_TOKEN: c920e2e7d8eb006eea138a4a8cefa9286e86b4fbd295f9e1049c44907222388e

language: php
php:
  - '7.1'

install:
  - curl -s http://getcomposer.org/installer | php
  - php composer.phar install --no-interaction

before_script:
  - composer install

script:
  - mkdir -p build/logs
  - php vendor/bin/phpunit -c phpunit.xml

after_success:
  - travis_retry php vendor/bin/coveralls

notifications:
  email:
    - coding@sebastianbrosch.de